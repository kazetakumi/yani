---
name: Card
description: A container with card styling around exactly one child. Use to visually group one block of related content.
---

# Card

Exactly one child: to show multiple elements inside a card, point `child` at a Column that holds them.
