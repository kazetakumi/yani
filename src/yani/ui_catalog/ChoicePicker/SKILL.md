---
name: ChoicePicker
description: Select one or more options from a list, as checkboxes or chips. Use for constrained choices instead of free text.
---

# ChoicePicker

`options` are fixed structure; the selection is written to `value`'s path as an array of the selected option values.
