---
name: Definition
description: A boxed callout pinning something down in one or two sentences. Variants: plain (warm tan — new vocabulary), model (sage — the mental model), break (red dashed — where an analogy fails).
---

A boxed callout pinning something down in one or two sentences. Variants: plain (warm tan — new vocabulary), model (sage — the mental model), break (red dashed — where an analogy fails).
