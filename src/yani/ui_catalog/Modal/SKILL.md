---
name: Modal
description: A dialog window opened by a trigger component. Use for content that should overlay the page on demand.
---

# Modal

Both `trigger` and `content` are ids of components defined elsewhere in the list — never inline. The trigger renders in place; clicking it opens the dialog.
