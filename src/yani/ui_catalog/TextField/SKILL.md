---
name: TextField
description: A field for user text input. Use to collect free-form text, numbers or passwords from the user.
---

# TextField

Two-way binding is client-local: typing updates the surface's data model at `value`'s path, and the server only learns of it when a future action fires.
